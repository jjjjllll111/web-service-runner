name: Relay

on:
  workflow_run:
    workflows: ["Run Service"]
    types: [completed]

permissions:
  actions: write
  contents: read

concurrency:
  group: relay-group
  cancel-in-progress: true

jobs:
  relay:
    runs-on: ubuntu-latest
    steps:
      - name: Wait for run to fully finish
        run: |
          echo "Waiting for previous run to fully clean up..."
          sleep 60

          API_TOKEN="${{ secrets.UPLOAD_API_TOKEN }}"
          OWNER="${{ github.repository_owner }}"
          REPO="${{ github.event.repository.name }}"

          # 确认没有 in_progress 的 run
          for i in $(seq 1 10); do
            COUNT=$(curl -sf -H "Authorization: Bearer $API_TOKEN" \
              -H "Accept: application/vnd.github.v3+json" \
              "https://api.github.com/repos/$OWNER/$REPO/actions/runs?workflow_id=run.yml&status=in_progress&per_page=5" | \
              jq '.total_count' 2>/dev/null || echo "0")
            if [ "$COUNT" = "0" ]; then
              echo "Previous run fully finished"
              break
            fi
            echo "Still running ($COUNT), waiting 30s..."
            sleep 30
          done

      - name: Trigger next run
        run: |
          echo "Triggering next run..."
          curl -sf -X POST \
            -H "Authorization: Bearer ${{ secrets.UPLOAD_API_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Content-Type: application/json" \
            -d '{"ref":"main"}' \
            "https://api.github.com/repos/${{ github.repository_owner }}/${{ github.event.repository.name }}/actions/workflows/run.yml/dispatches"
          echo "Next run triggered!"
